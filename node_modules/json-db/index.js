var fs = require('fs');

exports.InsertIntoDatabase = function(db_name, json){
    fs.readFile('database/' + db_name + '.json', function(err,data){
        if(err){
            console.log('Does database exist? If so, I am going to create one for you!');
            
            // logic
        } else {
            // logic
        }
    })
}